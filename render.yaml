services:
  - type: web
    name: lecture-helper-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --timeout 120 --workers 1
    envVars:
      - key: PORT
        value: 10000
